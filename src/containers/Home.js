import Search from '../components/Home/Search';
import Landing from '../components/Home/Landing';

const Home = () => {
    if (true) {
        return (
            <Search />
        )
    } else {
        return (
            <Landing />
        )
    }
}

export default Home;
